{
  "name": "exercices-error404",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "sass": "^1.68.0"
  }
}
